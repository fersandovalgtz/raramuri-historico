let entries=[]; let direction='ALL'; let conf='ALL';
const norm=s=>String(s||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/ſ/g,'s').toLowerCase();
async function init(){entries=await fetch('data/entries.json').then(r=>r.json()); document.getElementById('count').textContent=entries.length.toLocaleString('es-MX'); render();}
function setDir(d,b){direction=d;document.querySelectorAll('.filters:not(.conf) button').forEach(x=>x.classList.remove('active'));b.classList.add('active');render()}
function setConf(d,b){conf=d;document.querySelectorAll('.filters.conf button').forEach(x=>x.classList.remove('active'));b.classList.add('active');render()}
function label(e){if(e.segmentation_confidence==='curated_anchor')return 'Ancla curada · cotejo final pendiente';if(e.segmentation_confidence==='high_machine')return 'Segmentación automática · confianza alta';if(e.segmentation_confidence==='medium_machine')return 'Segmentación automática · confianza media';return 'Segmentación automática · confianza baja';}
function render(){const q=norm(document.getElementById('q').value.trim());let rows=entries.filter(e=>(direction==='ALL'||e.direction===direction)&&(conf==='ALL'||e.segmentation_confidence===conf)&&(!q||norm([e.headword_raw,e.headword_ocr_raw,e.definition_raw,e.translation_es_editorial].join(' ')).includes(q)));document.getElementById('shown').textContent=rows.length.toLocaleString('es-MX');const box=document.getElementById('results');if(!rows.length){box.innerHTML='<div class="empty">No hay coincidencias con estos filtros.</div>';return;}box.innerHTML=rows.slice(0,100).map(e=>`<article class="card"><div><div class="head">${esc(e.headword_raw)}</div><div class="gloss">${esc(e.definition_raw||'')}</div>${e.translation_es_editorial?`<div class="es"><strong>Español editorial:</strong> ${esc(e.translation_es_editorial)}</div>`:''}<span class="flag">${esc(label(e))}</span></div><div class="meta">${e.direction}<br>Steffel 1809 · p. ${esc(e.printed_page)}<br>${esc(e.record_id)}<br>OCR ${esc(e.source_ocr_line_start)}–${esc(e.source_ocr_line_end)}</div></article>`).join('')}
function esc(s){return String(s||'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}
document.addEventListener('DOMContentLoaded',()=>{document.getElementById('q').addEventListener('input',render);init()});