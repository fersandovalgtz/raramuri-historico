# Auditoría semántica v2 de los 59 pares modernos exactos `X ~ X+ami`

**Fecha:** 2026-08-13. **Corpus:** Rarámuri Digital SRC-02. **Estatus:** capa analítica IA-asistida; revisión humana pendiente. **Sustituye:** `MODERN_EXACT_BASE_AMI_SEMANTIC_TRIAGE.md` para los conteos analíticos vigentes.

## Corrección respecto de v1

La primera auditoría dejó `Comíami` ‘cóncavo, alabeado’ entre los casos semánticamente relacionados pero con ruta derivativa no adjudicada. Una revisión posterior de la anotación fuente-explícita mostró dos datos independientes dentro de SRC-02: el registro `Comíami` está clasificado como `Pp` y una entrada de la misma familia consigna literalmente `pp: comíami`. El caso debe, por tanto, pasar al subconjunto transparente/fuente-apoyado.

Los conteos vigentes son:

- **49/59** pares exactos con relación semántica transparente o apoyada explícitamente por la fuente;
- **3/59** relaciones semánticas plausibles cuya ruta derivativa sigue abierta;
- **7/59** emparejamientos exactos mecánicos no derivacionales.

No cambia la conclusión metodológica central: coincidencia exacta de cadena no equivale a derivación validada.

## Caso promovido: `Comí → Comíami`

SRC-02 registra `Comí` con los sentidos ‘estar bajo’ y ‘hacer arroyo’; `Comíami` significa ‘cóncavo, alabeado’ y está clasificado `Pp`. Otra entrada de la misma familia identifica explícitamente `comíami` como participio. La relación estado/configuración → propiedad resultante queda, por ello, mejor fundada de lo que indicaba la versión inicial.

## Tres casos todavía abiertos

`Co'huáami` ‘alimento, comida’ está semánticamente relacionado con `Co'huá` ‘comer’, pero la propia fuente presenta `co'huéami` como participio de `Co'huá`; el emparejamiento exacto `Co'huá ~ Co'huáami` no debe convertirse automáticamente en ese análisis.

`Jubáami` ‘al fin, finalmente, último’ podría relacionarse con `Jubá` ‘atrás’ mediante una extensión espacial-temporal, pero la fuente presenta homonimia adicional y no se adjudica aquí la ruta.

`Rasáami` ‘blando’ es compatible con un estado resultante de `Rasá` ‘machucarse’, pero requiere confirmación lingüística independiente.

## Siete emparejamientos mecánicos que permanecen fuera

Se conservan como falsos pares exactos o controles `Huiráami`, `Machíriami`, `Napóami`, `Ohuáami`, `Ritéami`, `Siríami` y `Suníami` respecto de las bases exactas que produjo la prueba mecánica. Esto no invalida relaciones diacrónicas no exactas: `Siríami`, por ejemplo, sigue siendo un candidato comparativo de prioridad alta por `Seliameke` histórico y `siˈríame` de Choguita, pero **no** por los homónimos modernos `Siri` ‘tijera/llegaron’.

## Consecuencia

La matriz funcional histórica-moderna debe recalcularse sobre **49**, no 48, pares modernos transparentes. `Comíami` entra en el dominio amplio de estado/configuración → propiedad/resultativo. La corrección es pequeña numéricamente, pero importante metodológicamente: cuando aparece evidencia explícita posterior, la clasificación previa se revisa y queda trazabilidad de la versión sustituida.

`ai_assisted=true`; `human_reviewed=false`; `automatic_morpheme_assignment=false`; `cognacy_judgment=not_performed`; `historical_continuity_judgment=not_performed`.
