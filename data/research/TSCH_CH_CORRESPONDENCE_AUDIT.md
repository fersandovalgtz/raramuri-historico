# Auditoría formal `<tsch>` histórico ~ `ch/tʃ` moderno

**Fecha:** 2026-08-13.  
**Candidato objetivo:** `Tſchiperameke ~ Chipérami/chiperami`.  
**Estatus:** correspondencia de la africada fuertemente establecida; candidato elevado a grado A de arquitectura evidencial, sin declarar cognación plena.

## 1. `<tsch>` no es una dificultad ortográfica sin resolver

Merrill dedica una sección específica a las africadas de Steffel. Su análisis establece que `<tsch>` representa la africada palatoalveolar sorda `[č]`; `<tsch>` y `<ts>` se interpretan históricamente como alófonos de un solo fonema `/č/`.

El apéndice fonético transcribe de manera explícita:

`tschipérameke || čipérameke`.

Por tanto, el comienzo de `Tſchiperameke` no debe compararse ingenuamente como letras alemanas `tsch-` frente a `ch-` moderno. La comparación pertinente comienza con una africada histórica `č-`.

## 2. Dos controles independientes antes de usar la correspondencia en el candidato

La equivalencia histórica `<tsch>/č` y moderna `ch/tʃ` no se deduce de `Tſchiperameke` mismo.

### Familia ‘dormir’

Merrill transcribe `cotschí || kočí`. Caballero, en una variedad moderna independiente, analiza directamente la raíz `kotʃi` ‘dormir’. El mismo núcleo lexical proporciona, por tanto, una correspondencia histórica `č` ~ moderna `tʃ` fuera del candidato `Tſchiper`.

### Familia ‘negro’

Merrill registra y transcribe el histórico `tschócameke || čókameke`. El *Compendio básico de la gramática ralámuli* de 1997 documenta en varias regiones modernas `chó-ka-mi`, `chó-ka-me` y `chó-ko-m` para negro. Esta segunda familia vuelve a poner la africada histórica y el moderno `ch-` en el mismo dominio lexical sin depender de `Tſchiper`.

Con dos familias de control independientes, la correspondencia de la consonante inicial del candidato deja de ser ad hoc.

## 3. Forma objetivo

La comparación normalizada es:

**histórico:** `čipérameke`  
**moderno SRC-02:** `Chipérami`  
**moderno comunitario:** `chiperami`

En la forma histórica y en SRC-02 el acento recae en la misma sílaba del material lexical: `-pér-`. Descriptivamente, la secuencia anterior al dominio sufijal general presenta una similitud extraordinaria:

`čipér- ~ chipér-`.

No se declara aquí un lema histórico desnudo `čipér`, porque la auditoría no ha localizado todavía una atestación independiente de esa base. Se habla de **secuencia de tema o material pre-sufijal**, no de un morfema inventado por recorte.

## 4. Semántica

Steffel glosa `Tſchiperameke` dentro del campo `Fein. Dünn. Flach.` —fino, delgado, plano—. SRC-02 define `Chipérami` como ‘Plano’ y lo clasifica `Pp`.

La evidencia moderna fuera de SRC-02 añade contexto. En una adivinanza comunitaria de Guerrero, `chiperami` aparece en la línea rarámuri correspondiente a “parezco tablilla”; por prudencia, el texto se interpreta como contexto compatible con una propiedad plana/delgada, no como una definición diccionarial.

Dos usos adicionales refuerzan el mismo campo semántico, aunque no aumentan automáticamente la independencia documental. El diccionario actualizado de 2016 traduce una construcción con `chipérami` como ‘con un palo ancho’. Un corpus público moderno en rarámuri utiliza `kawí chipérami` en el contexto de la antigua creencia de que la tierra era plana y lo contrasta con `chitúlami`, de forma circular/redonda.

## 5. Evaluación

El candidato reúne ahora:

- valor fonético histórico de `<tsch>` explícitamente analizado por Merrill;
- dos familias de control independientes para `č ~ ch/tʃ` antes de aplicar la correspondencia al objetivo;
- secuencia lexical casi idéntica `čipér- ~ chipér-`;
- acento coincidente sobre `-pér-`;
- campo semántico estable ‘fino/delgado/plano/ancho-tabular’;
- un testigo comunitario moderno independiente de la línea SRC-02/Hilton;
- clasificación `Pp` moderna dentro de SRC-02.

Por arquitectura de evidencia, el candidato asciende de **A- a A**.

Ese grado no es una probabilidad de cognación. La historia exacta de la terminación `-ameke ~ -ami` sigue perteneciendo al problema morfológico general del proyecto y no se declara resuelta para este lexema.

## 6. Límites

No se convierte la ortografía de SRC-02 en una transcripción fonémica validada. No se asigna automáticamente un morfema `čipér-` si una fuente no lo segmenta. No se transfiere la etiqueta `Pp` de SRC-02 al texto comunitario. No se cuentan el diccionario de 2016 ni el corpus religioso como nuevas replicaciones independientes sin auditar sus genealogías léxicas.

La conclusión formal restringida es más fuerte y defendible:

> El material lexical histórico `čipér-` visible en `čipérameke` y el moderno `chipér-/chiper-` presentan una correspondencia formal y acentual extremadamente estrecha, y la africada histórica está sustentada por correspondencias independientes en otras familias. La continuidad lexical es, por ello, una hipótesis de prioridad máxima; la demostración histórica completa requiere todavía integrar esa raíz con la reconstrucción general del dominio participial `-ameke/-ame/-ami`.

`ai_assisted=true`; `merrill_tsch_analysis_verified=true`; `independent_correspondence_controls=2`; `independent_modern_community_witness=true`; `human_reviewed_by_project=false`; `automatic_morpheme_assignment=false`; `automatic_sound_law=false`; `cognacy_judgment=not_performed`; `historical_continuity_judgment=not_performed`.
