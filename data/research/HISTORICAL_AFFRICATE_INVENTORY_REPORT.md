# Inventario histórico completo de `<tsch>` / `<ts>` en RAR–DE

**Corte:** 2026-08-13. **Estatus:** inventario grafemático reproducible; no es reconstrucción fonológica.

Se localizaron **157 ocurrencias** en **146 componentes** de **139 registros RAR–DE activos**. **155** ocurrencias tienen vocal inmediatamente después y entran en la prueba distribucional simple de Merrill; **2** contextos no vocálicos se conservan fuera de esa prueba.

## Distribución por grafema y posición

| Grafema / posición | Ocurrencias |
|---|---:|
| `ts` / initial | 14 |
| `ts` / medial | 36 |
| `tsch` / initial | 39 |
| `tsch` / medial | 68 |

## Contraste con Merrill 2020 §7.5.2

Se codificó literalmente: variación inicial `<tsch> ~ <ts>` ante `a`, `<ts>` ante `e`, `<tsch>` ante `i/o/u`, y variación de ambos en posición medial **ante vocales**. Los contextos no vocálicos no se fuerzan dentro de ese modelo.

Excepciones iniciales potenciales en la capa diplomática: **1**.

| Forma | Grafema | Vocal siguiente | Página |
|---|---|---|---:|
| `Tſchécemeke` | `tsch` | `e` | 368 |

Contextos fuera de la regla vocálica simple: `Hatſca` (ts+c), `Tetshóje` (ts+h).

## Sensibilidad facsimilar

La única excepción diplomática (`Tſchécemeke`) fue revisada contra el facsímil. La lectura IA-asistida propuesta es `Tſchócameke`. Sin modificar la fuente diplomática, esa lectura cambia el entorno a `<tsch>+o` y reduce las excepciones iniciales de **1 a 0**. La propuesta permanece pendiente de revisión humana.

Las formas diplomáticas no se modifican.

`automatic_phonological_interpretation=false`; `automatic_sound_correspondence_inference=false`; `human_reviewed=false`.
